# @glasskit.ai/create

WIP create package for GlassKit starter projects.

This first version only copies the Rokid Glasses starter app. The interface and generated project are work in progress and may change.

## Usage

```bash
npm create @glasskit.ai
npm create @glasskit.ai my-rokid-app
```

The default target directory is `rokid-starter`.
