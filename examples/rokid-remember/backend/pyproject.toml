[project]
name = "backend"
version = "0.1.0"
description = "Rokid recorder backend"
readme = "README.md"
requires-python = ">=3.12.12"
dependencies = [
    "fastapi[standard]>=0.115.14",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
    "ty>=0.0.14",
]
