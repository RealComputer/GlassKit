[project]
name = "backend"
version = "0.1.0"
description = "Rokid Overshoot signaling and event bridge"
readme = "README.md"
requires-python = ">=3.12.12"
dependencies = [
    "fastapi[standard]>=0.115.14",
    "httpx>=0.28.1",
    "websockets>=16.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
    "ty>=0.0.14",
]
